-- =====================================================
-- SQLPub 数据库连接测试 SQL
-- =====================================================
-- 使用方法：
-- 1. 使用 SQLPub 控制台或 MySQL 客户端连接数据库
-- 2. 执行以下命令测试连接

-- 测试数据库连接
SELECT '连接成功' as status, NOW() as current_time;

-- 查看当前数据库
SELECT DATABASE() as current_database;

-- 查看所有表
SHOW TABLES;

-- 测试查询博客文章
SELECT COUNT(*) as blog_count FROM blog_posts;

-- 测试查询产品
SELECT COUNT(*) as product_count FROM products;

-- 测试查询用户
SELECT COUNT(*) as user_count FROM users;

-- =====================================================
-- 连接信息检查清单
-- =====================================================
-- 主机: mysql6.sqlpub.com
-- 端口: 3311
-- 用户: apollohzl
-- 数据库: hzliflow

-- =====================================================
-- 常见连接问题排查
-- =====================================================
-- 1. 连接超时 (ETIMEDOUT):
--    - 检查网络连接是否正常
--    - 检查 SQLPub 服务器是否在线
--    - 检查防火墙设置
--    - 检查端口 3311 是否被阻止

-- 2. 认证失败:
--    - 检查用户名和密码是否正确
--    - 检查用户权限设置

-- 3. 数据库不存在:
--    - 检查数据库名称是否正确
--    - 确认数据库已创建

-- 4. 连接数限制:
--    - 检查连接池配置
--    - 检查 SQLPub 账户的连接数限制

-- =====================================================
-- 测试命令示例
-- =====================================================
-- 使用 mysql 命令行工具测试:
-- mysql -h mysql6.sqlpub.com -P 3311 -u apollohzl -p hzliflow

-- 使用 node.js 测试:
-- node test_sql_connection.js

-- =====================================================
-- 性能优化建议
-- =====================================================
-- 1. 使用连接池而不是单个连接
-- 2. 设置合理的连接超时时间
-- 3. 实现查询重试机制
-- 4. 监控连接池状态
-- 5. 定期测试数据库连接